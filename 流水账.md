# 操作流水账

本文档记录了从对话开始到结束的所有操作步骤、执行的命令、以及每步的用途和输出。

---

## 一、文档改写操作（TECHNICAL.md）

### 1.1 读取现有文档
- **执行的命令：** `Read` 工具，读取 `TECHNICAL.md`（2046 行）以及多个源代码文件（CorsConfig.java、JwtAuthFilter.java、SecurityConfig.java、JwtUtil.java、ReviewController.java、ReviewService.java）
- **用途：** 了解现有文档结构，获取源代码内容用于后续改写
- **输出：** 文档内容 + 源码内容

### 1.2 首次大规模内容插入
- **执行的命令：** 使用 `task` 工具（子代理）执行内容插入
- **用途：** 在 TECHNICAL.md 中插入 5 个新章节（3.4 其他框架总览、4.3 CorsFilter、4.4 JwtAuthFilter、4.5 DispatcherServlet、5.7 ReviewController 全流程注解、5.8 Spring Boot 启动流程、13. 其他框架体系）
- **输出：** 文件从 2046 行扩展到 2873 行（+827 行）

### 1.3 验证插入结果
- **执行的命令：** `bash wc -l`、`grep` 提取标题行、`Read` 读取关键段落
- **用途：** 确认新内容正确插入，格式正常
- **输出：** 86 个标题覆盖所有章节，内容完整

### 1.4 重写 2.1 节（核心框架）
- **执行的命令：** `Read` 读取 pom.xml、WordLearningApplication.java、User.java、Word.java、UserRepository.java、AuthService.java、SecurityConfig.java、CorsConfig.java、application.yml、AuthController.java
- **用途：** 获取项目实际代码用于 2.1 节的深度讲解
- **执行的写入命令：** `Edit` 工具替换 2.1 节全部内容
- **输出：** 2.1 节从约 160 行扩展到约 435 行，新增 Spring Boot 原理、JPA 三种查询方式、Security 三层模型、Validation 完整流程

### 1.5 重写 5.8 节（Spring Boot 启动流程）
- **执行的命令：** `Read` 读取当前 5.8 节内容（约 75 行）
- **执行的写入命令：** `Edit` 工具替换为约 660 行的详细版本
- **输出：** 包含 Mermaid 总览图、16 步启动时序、源码级解释、本项目 Bean 创建顺序、Tomcat 启动细节、Filter 注册顺序、排错指南
- **文件最终行数：** 3790 行

### 1.6 第一次提交
- **执行的命令：**
  ```bash
  git add english/word-learning-backend/TECHNICAL.md && git commit -m "..."
  ```
- **用途：** 提交 2.1 和 5.8 的重写
- **输出：** 1798 行新增，54 行删除，commit hash `7d729a6`

### 1.7 重写 2.1.3（Spring Security 详解）
- **执行的命令：** `Read` JwtAuthFilter.java 源码
- **用途：** 获取过滤器源码用于深度讲解
- **执行的写入命令：** `Edit` 工具替换 2.1.3 全部内容
- **输出：** 新增 5 个子章节：
  - 3.1 JwtAuthFilter 创建注入（@Component → SecurityConfig → addFilterBefore 全链路）
  - 3.2 SecurityConfig 两个 @Bean（filterChain 和 passwordEncoder 的参数来源、执行时机）
  - 3.3 15 个 Filter 完整列表（序号、创建时机、各自职责、执行示例）
  - 3.4 业务代码取用户方式
  - 3.5 HttpSecurity 常用 API 速查表（15 个方法）

### 1.8 重写 2.1.5（请求全链路 20 步）
- **执行的命令：** `Edit` 工具替换原有四行概要为 8 阶段 20 步详解
- **输出：** 包含：
  - 阶段一：Tomcat TCP 接收原始 HTTP 请求
  - 阶段二：Filter 链执行（7 个 Security Filter 逐层经过）
  - 阶段三：DispatcherServlet.doDispatch() 入口源码
  - 阶段四：HandlerMapping 匹配（MappingRegistry 内部结构）
  - 阶段五：HandlerAdapter 参数解析（26 个 ArgumentResolver 遍历决策树、@Valid 校验失败处理）
  - 阶段六：反射调用 Controller 方法
  - 阶段七：ReturnValueHandler（15 个处理器、Jackson 序列化）
  - 阶段八：响应写回客户端
  - Mermaid 时序图

### 1.9 第二次提交
- **执行的命令：**
  ```bash
  git add english/word-learning-backend/TECHNICAL.md && git commit -m "..."
  ```
- **用途：** 提交 2.1.3 和 2.1.5 的重写
- **输出：** 913 行新增，112 行删除，commit hash `1f938a6`

---

## 二、数据库操作

### 2.1 清空 word_learning 数据库
- **用户请求：** "清空word_learning数据库中的所有表"
- **用途：** 删除 word_learning 数据库下的所有表

#### 2.1.1 读取数据库连接配置
- **执行的命令：** `grep -A5 'datasource:'` 读取 application.yml
- **用途：** 获取 MySQL 地址（localhost:3306）、用户名（root）、密码（123456）
- **输出：**
  ```
  datasource:
    url: jdbc:mysql://localhost:3306/word_learning?...
    username: root
    password: 123456
  ```

#### 2.1.2 尝试 Linux 端 MySQL 连接
- **执行的命令：** `mysql -uroot -p123456 word_learning -e "SELECT ..."`
- **用途：** 检查是否能从 WSL Linux 直接连接 MySQL
- **输出：** `/bin/bash: line 1: mysql: command not found`

#### 2.1.3 尝试其他连接方式
- **执行的命令：** 检查 `docker ps`、`systemctl status mysql`、`which mysqld`、`apt list --installed | grep mysql`
- **用途：** 寻找可用的 MySQL 客户端
- **输出：** 全为空，说明 Linux 端没有安装 MySQL

#### 2.1.4 尝试安装 MySQL 客户端
- **执行的命令：** `sudo apt-get install -y mysql-client`
- **用途：** 安装 MySQL 命令行工具
- **输出：** `sudo: A terminal is required to authenticate`（无 sudo 权限）

#### 2.1.5 尝试 Python 连接
- **执行的命令：** `python3 -c "import pymysql"`、`python3 -m pip install --user pymysql`
- **用途：** 尝试用 Python 连接 MySQL
- **输出：** 无 pymysql 模块，无 pip 命令

#### 2.1.6 查找 Windows 端 MySQL
- **执行的命令：** `ls /mnt/c/Program\ Files/MySQL/`
- **用途：** 检查 Windows 主机是否安装了 MySQL
- **输出：**
  ```
  Connector C++ 8.0
  Connector ODBC 8.0
  Connector Python 8.0
  MySQL Router 8.0
  MySQL Server 8.0
  MySQL Shell 8.0
  MySQL Workbench 8.0
  ```

#### 2.1.7 通过 Windows 端 mysql.exe 列出表
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 word_learning \
    -e "SELECT CONCAT('DROP TABLE IF EXISTS ', table_name, ';') \
        FROM information_schema.tables WHERE table_schema = 'word_learning';"
  ```
- **用途：** 从 Windows 端的 MySQL 列出所有表
- **输出：**
  ```
  DROP TABLE IF EXISTS collocations;
  DROP TABLE IF EXISTS definitions;
  DROP TABLE IF EXISTS examples;
  DROP TABLE IF EXISTS prep_patterns;
  DROP TABLE IF EXISTS usage_notes;
  DROP TABLE IF EXISTS word_forms;
  DROP TABLE IF EXISTS word_relations;
  DROP TABLE IF EXISTS word_tags;
  DROP TABLE IF EXISTS word_variants;
  DROP TABLE IF EXISTS words;
  ```
  **共计 10 张表。**

#### 2.1.8 执行删除操作
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 word_learning \
    -e "SET FOREIGN_KEY_CHECKS = 0; \
        DROP TABLE IF EXISTS usage_notes; \
        DROP TABLE IF EXISTS prep_patterns; \
        DROP TABLE IF EXISTS examples; \
        DROP TABLE IF EXISTS collocations; \
        DROP TABLE IF EXISTS definitions; \
        DROP TABLE IF EXISTS word_variants; \
        DROP TABLE IF EXISTS word_tags; \
        DROP TABLE IF EXISTS word_relations; \
        DROP TABLE IF EXISTS word_forms; \
        DROP TABLE IF EXISTS words; \
        SET FOREIGN_KEY_CHECKS = 1; \
        SHOW TABLES;"
  ```
- **用途：** 先禁用外键检查 → 按顺序删除所有表 → 恢复外键检查 → 验证是否已清空
- **输出：** 仅显示 `mysql: [Warning] Using a password on the command line interface can be insecure.` 警告，无其余输出（说明表已删除成功，SHOW TABLES 返回空）

#### 2.1.9 验证删除结果
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 word_learning -e "SHOW TABLES;"
  ```
- **用途：** 确认数据库已完全清空
- **输出：** 无内容（空结果集），确认 10 张表已全部删除

---

## 三、文件最终状态

| 指标 | 数值 |
|---|---|
| TECHNICAL.md 最终行数 | 4591 行 |
| 总提交次数 | 2 次 |
| 第一次 commit hash | `7d729a6` |
| 第二次 commit hash | `1f938a6` |
| 总新增行数 | 1798 + 913 = 2711 行 |
| 总删除行数 | 54 + 112 = 166 行 |
| 数据库清空表数 | 10 张 |

---

## 四、被删除的数据库表清单

| 序号 | 表名 | 说明 |
|---|---|---|
| 1 | `words` | 单词主表 |
| 2 | `definitions` | 单词释义 |
| 3 | `collocations` | 搭配短语 |
| 4 | `examples` | 例句 |
| 5 | `prep_patterns` | 介词搭配模式 |
| 6 | `usage_notes` | 用法说明 |
| 7 | `word_forms` | 词形变化 |
| 8 | `word_relations` | 单词关系 |
| 9 | `word_tags` | 单词标签 |
| 10 | `word_variants` | 单词变体 |

---

## 五、从 schema.sql 重建数据库表

- **用户请求：** 把 `english/database/schema.sql` 里的表创建到数据库中
- **用途：** 重建 word_learning 数据库的 36 张表，并插入示例数据

### 5.1 读取 SQL 文件
- **执行的命令：** `Read` 工具读取 `/mnt/d/opencode/study/english/database/schema.sql`
- **用途：** 确认建表 SQL 内容
- **输出：** SQL 文件共 870+ 行，包含 36 张 CREATE TABLE + 索引 + 示例数据 INSERT

### 5.2 首次执行 schema.sql
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 < "/mnt/d/opencode/study/english/database/schema.sql"
  ```
- **用途：** 直接执行 SQL 文件
- **输出：**
  ```
  ERROR 1406 (22001) at line 737: Data too long for column 'nickname' at row 1
  ```
- **原因：** mysql.exe 默认字符集不是 utf8mb4，导致中文写入失败

### 5.3 检查表结构
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 --default-character-set=utf8mb4 word_learning \
    -e "SHOW CREATE TABLE users\G"
  ```
- **用途：** 确认 users 表结构是否正常
- **输出：** `nickname varchar(100) DEFAULT NULL` — 列定义正确，是字符集问题

### 5.4 测试单条 INSERT
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 --default-character-set=utf8mb4 word_learning \
    -e "INSERT INTO users (...) VALUES (...);"
  ```
- **用途：** 测试加上 `--default-character-set=utf8mb4` 后能否正常插入中文
- **输出：** 无错误（之前失败的语句现在成功了）

### 5.5 重新执行 schema.sql（带 utf8mb4）
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 --default-character-set=utf8mb4 \
    < "/mnt/d/opencode/study/english/database/schema.sql"
  ```
- **用途：** 加上 utf8mb4 字符集参数重新执行建库 SQL
- **输出：** 仅 `Warning` 提示密码在命令行可见，无错误

### 5.6 验证建表结果
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 --default-character-set=utf8mb4 word_learning \
    -e "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'word_learning';"
  ```
- **用途：** 统计数据库中表的数量
- **输出：** `table_count = 36`（全部创建成功）

### 5.7 验证示例数据
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 --default-character-set=utf8mb4 word_learning \
    -e "SELECT COUNT(*) AS users FROM users; SELECT COUNT(*) AS words FROM words; SELECT TABLE_NAME FROM information_schema.tables WHERE table_schema = 'word_learning' ORDER BY TABLE_NAME;"
  ```
- **用途：** 确认示例数据已插入，并列出所有表名
- **输出：**
  ```
  users: 2（admin, demo）
  words: 1（abandon）
  tables: 36 张表完整列出
  ```

### 5.8 36 张表完整清单

| 序号 | 表名 | 说明 |
|---|---|---|
| 1 | `articles` | 英文文章表 |
| 2 | `badges` | 徽章定义表 |
| 3 | `collocations` | 固定搭配表 |
| 4 | `content_ratings` | 内容评分/反馈表 |
| 5 | `daily_plan_items` | 每日计划词条表 |
| 6 | `daily_recommendations` | 每日推荐表 |
| 7 | `definitions` | 释义表（一词多义） |
| 8 | `examples` | 例句表 |
| 9 | `favorite_folders` | 收藏夹表 |
| 10 | `favorites` | 收藏条目表 |
| 11 | `learning_activities` | 学习活动日志表 |
| 12 | `learning_plans` | 学习计划模板表 |
| 13 | `prep_patterns` | 介词搭配表 |
| 14 | `reading_progress` | 文章阅读进度表 |
| 15 | `review_log` | 答题日志表 |
| 16 | `search_history` | 搜索历史表 |
| 17 | `study_strategies` | 学习策略表 |
| 18 | `usage_notes` | 用法说明表 |
| 19 | `user_badges` | 用户获得徽章表 |
| 20 | `user_daily_plan_entries` | 用户每日计划条目表 |
| 21 | `user_entity_tags` | 用户标签与实体关联表 |
| 22 | `user_frequencies` | 个人频率表 |
| 23 | `user_notes` | 用户个人笔记表 |
| 24 | `user_plans` | 用户学习计划表 |
| 25 | `user_settings` | 用户设置表 |
| 26 | `user_stats` | 用户统计/游戏化数据表 |
| 27 | `user_tags` | 用户自定义标签表 |
| 28 | `user_word_book_progress` | 用户单词本学习进度表 |
| 29 | `users` | 用户表 |
| 30 | `word_book_entries` | 单词本词条表 |
| 31 | `word_books` | 单词本表 |
| 32 | `word_forms` | 单词变形表 |
| 33 | `word_relations` | 单词关系网络表 |
| 34 | `word_tags` | 单词标签表 |
| 35 | `word_variants` | 拼写变体表 |
| 36 | `words` | 词汇主表（含 SM-2 间隔重复） |

---

## 六、修复 FavoriteRepository 启动报错

### 6.1 错误信息
- **用户请求：** 粘贴了 Spring Boot 启动报错堆栈
- **错误摘要：**
  ```
  UnsatisfiedDependencyException: Error creating bean with name 'favoriteController'
    → favoriteService → favoriteRepository
    → Could not create query for FavoriteRepository.findByFolderIdAndUserId(Long, Long)
    → No property 'userId' found for type 'Favorite'
  ```

### 6.2 错误原因
- `FavoriteRepository` 中有方法 `findByFolderIdAndUserId(Long folderId, Long userId)` 和 `findByUserIdAndEntityTypeAndEntityId(Long, EntityType, Long)`
- `FavoriteService` 中多处使用 JPQL 查询 `fv.userId`
- 但 `Favorite.java` 实体类**没有 `userId` 字段**
- `favorites` 数据库表也**没有 `user_id` 列**

### 6.3 修复步骤

#### 6.3.1 读取源码确认
- **执行的命令：** `Read` 工具读取 `Favorite.java`、`FavoriteRepository.java`、`FavoriteService.java`
- **用途：** 确认错误根因
- **输出：** Favorite 实体只有 `folderId`、`entityType`、`entityId`、`note`，没有 `userId`

#### 6.3.2 修复 Entity（Favorite.java）
- **执行的命令：** `Edit` 工具在 `Favorite.java` 中 `folderId` 字段前插入 `userId` 字段
- **旧代码：**
  ```java
  @Column(name = "folder_id", nullable = false)
  private Long folderId;
  ```
- **新代码：**
  ```java
  @Column(name = "user_id", nullable = false)
  private Long userId;

  @Column(name = "folder_id", nullable = false)
  private Long folderId;
  ```

#### 6.3.3 修复数据库表
- **首次尝试（无数据检查）：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 --default-character-set=utf8mb4 word_learning \
    -e "ALTER TABLE favorites ADD COLUMN user_id INT NOT NULL \
        COMMENT '→ users.id' AFTER folder_id, ADD INDEX idx_user (user_id);"
  ```
- **输出：** 成功（MySQL 对已有 1 行数据自动填充了默认值 0）

#### 6.3.4 修复已有数据
- **执行的命令：**
  ```bash
  "/mnt/c/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe" \
    -uroot -p123456 --default-character-set=utf8mb4 word_learning \
    -e "SELECT id, user_id, name FROM favorite_folders WHERE id = 1;"
  ```
- **输出：** folder_id=1 对应 user_id=1
- **执行的命令：** `UPDATE favorites SET user_id = 1 WHERE folder_id = 1;`
- **输出：** 成功

#### 6.3.5 同步修复 schema.sql
- **执行的命令：** `Edit` 工具在 `favorites` 表定义中插入 `user_id` 列，并更新 INSERT 语句
- **用途：** 保证下次用 schema.sql 重建时不再缺少该列

#### 6.3.6 编译验证
- **执行的命令：**
  ```bash
  cd /mnt/d/opencode/study/english/word-learning-backend && mvn compile -q
  ```
- **输出：** 无错误（编译通过）

#### 6.3.7 启动验证
- **执行的命令：**
  ```bash
  cd /mnt/d/opencode/study/english/word-learning-backend && \
    timeout 15 mvn spring-boot:run 2>&1 | tail -30
  ```
- **输出：** 应用正常启动（无 UnsatisfiedDependencyException）

---

### 6.4 涉及的文件

| 文件 | 修改类型 | 说明 |
|---|---|---|
| `word-learning-backend/.../entity/Favorite.java` | 修改 | 新增 `userId` 字段 + `@Column(name = "user_id")` |
| `english/database/schema.sql` | 修改 | 新增 `user_id INT NOT NULL` 列 + 索引 + 更新 INSERT |
| `word_learning.favorites` 表 | 修改 | ALTER TABLE 新增 `user_id` 列 |

---

## 七、前端与后端对接（移除所有 Mock 数据）

### 7.1 背景
前端 `english/frontend/` 是一个 Vue 3 + Vite 项目，原有 15 个页面 + 4 个 Pinia Store，**全部使用硬编码 Mock 数据**，没有发起过任何 HTTP 请求。后端 Spring Boot 有 17 个 Controller、72 个 API 端点，但前端从未调用过。

### 7.2 操作步骤

| 步骤 | 操作 | 涉及文件 |
|---|---|---|
| 1 | 创建 `src/api/http.js` — 基础 HTTP 客户端，自动注入 `Authorization: Bearer xxx` 头，处理 401 跳转登录，统一错误处理 | `api/http.js` |
| 2 | 创建 `src/api/index.js` — 72 个 API 端点封装（auth/user/word/search/review/article/folder/favorite/wordBook/plan/dashboard/leaderboard/wrongWord/tag/strategy/badge/admin） | `api/index.js` |
| 3 | 重写 `stores/user.js` — 登录/注册调用 `authApi.login/register`，存取 Token，`fetchProfile()` 恢复会话 | `stores/user.js` |
| 4 | 重写 `stores/words.js` — 搜索调用 `searchApi.search()`，单词详情调用 `wordApi.getDetail()`，笔记/标签/评分全走 API | `stores/words.js` |
| 5 | 重写 `stores/wordBooks.js` — 词书/策略/POS 分类从 `wordBookApi`/`strategyApi` 获取 | `stores/wordBooks.js` |
| 6 | 重写 `stores/dailyPlan.js` — 学习计划/每日词条/生成计划全走 `planApi` | `stores/dailyPlan.js` |
| 7 | 创建 `views/Login.vue` — 登录/注册双标签页，调用 `userStore.login/register`，成功跳转首页 | `views/Login.vue` |
| 8 | 更新 `router/index.js` — 添加 `/login` 路由 + `beforeEach` 导航守卫（无 Token 跳登录页） | `router/index.js` |
| 9 | 更新 `main.js` — 应用启动时检查 Token，存在则调用 `fetchProfile()` 恢复会话 | `main.js` |
| 10 | 添加 `vite.config.js` 中的 `@` 别名（`src/`） | `vite.config.js` |
| 11 | 更新 15 个视图页面，移除硬编码数据，改为 API 调用或 Store 读取 | 全部 `views/*.vue` |
| 12 | 更新 `AppLayout.vue` — 显示登录用户昵称，添加登出按钮 | `components/layout/AppLayout.vue` |

### 7.3 15 个视图的变更摘要

| 视图 | 原来数据源 | 新数据源 | 变更要点 |
|---|---|---|---|
| `Dashboard.vue` | 硬编码 stats + recommendations | `dashboardApi.get()` | 加载状态，空状态 |
| `Search.vue` | `wordStore.searchWords()` 只返回 "abandon" | `wordStore.search({ q })` | 实时搜索，搜索历史 API |
| `WordDetail.vue` | `wordStore.sampleWord` 永远显示 "abandon" | `wordStore.fetchWordDetail(id)` | 路由参数取 ID，加载/错误状态 |
| `Learning.vue` | `dailyPlanStore` 硬编码 14 个词 | `planApi.getDailyWords(date)` | 日期导航，加载状态 |
| `Review.vue` | 3 个硬编码单词 | `reviewApi.getQueue()` | 队列加载，提交结果 API |
| `Reading.vue` | 3 篇硬编码文章 | `articleApi.getList()` | 分页，筛选 |
| `ReadingArticle.vue` | 3 段硬编码正文 | `articleApi.getDetail(id)` | 进度保存，查词 API |
| `Favorites.vue` | 4 个硬编码文件夹 | `folderApi.getList()` | 增删改全部走 API |
| `FavoriteFolder.vue` | 3 条硬编码条目 | `folderApi.getItems(id)` | 复选框批量删除 |
| `WrongAnswers.vue` | 2 个硬编码错词 | `wrongWordApi.getList()` | 分页，题型筛选 |
| `WordBooks.vue` | 10 个硬编码预览词 | `wordBookStore.fetchBookWords(id)` | 筛选联动，加入计划 API |
| `StudyPlans.vue` | 3 个硬编码推荐计划 | `planApi.getTemplates()` | 加入计划、生成词表 |
| `Profile.vue` | 硬编码周数据/徽章/邮箱 | `userStore.fetchActivity(7)` etc | 活动图表、徽章列表、设置保存 |
| `Leaderboard.vue` | 4 个硬编码用户 | `leaderboardApi.get()` | 标签切换，我的排名 |
| `Admin.vue` | 2 个用户 + 2 个单词硬编码 | `adminApi.*` | 概览/用户/词库/反馈全走 API |

### 7.4 新增/修改的文件清单

| 文件 | 操作 | 行数 |
|---|---|---|
| `frontend/src/api/http.js` | 新增 | ~60 行 |
| `frontend/src/api/index.js` | 新增 | ~120 行 |
| `frontend/src/stores/user.js` | 重写 | ~100 行 |
| `frontend/src/stores/words.js` | 重写 | ~80 行 |
| `frontend/src/stores/wordBooks.js` | 重写 | ~60 行 |
| `frontend/src/stores/dailyPlan.js` | 重写 | ~70 行 |
| `frontend/src/views/Login.vue` | 新增 | ~130 行 |
| `frontend/src/router/index.js` | 修改 | +10 行 |
| `frontend/src/main.js` | 修改 | +10 行 |
| `frontend/vite.config.js` | 修改 | +4 行 |
| `frontend/src/views/Dashboard.vue` | 修改 | |
| `frontend/src/views/Search.vue` | 修改 | |
| `frontend/src/views/WordDetail.vue` | 修改 | |
| `frontend/src/views/Learning.vue` | 修改 | |
| `frontend/src/views/Review.vue` | 修改 | |
| `frontend/src/views/Reading.vue` | 修改 | |
| `frontend/src/views/ReadingArticle.vue` | 修改 | |
| `frontend/src/views/Favorites.vue` | 修改 | |
| `frontend/src/views/FavoriteFolder.vue` | 修改 | |
| `frontend/src/views/WrongAnswers.vue` | 修改 | |
| `frontend/src/views/WordBooks.vue` | 修改 | |
| `frontend/src/views/StudyPlans.vue` | 修改 | |
| `frontend/src/views/Profile.vue` | 修改 | |
| `frontend/src/views/Leaderboard.vue` | 修改 | |
| `frontend/src/views/Admin.vue` | 修改 | |
| `frontend/src/components/layout/AppLayout.vue` | 修改 | |
